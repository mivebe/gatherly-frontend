# Optional. Leave unset to use the local dev fallback in src/api/client.ts:
#   web/iOS simulator → http://localhost:4000/api
#   Android emulator  → http://10.0.2.2:4000/api
# Set this only to point at a non-local backend (e.g. prod):
# EXPO_PUBLIC_API_URL=https://gatherly-api.fly.dev/api
